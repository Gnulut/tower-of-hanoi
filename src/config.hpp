#pragma once

struct constants {
    static constexpr int FPS = 60;
    static constexpr int START_WINDOW_WIDTH = 1280; // in pixels
    static constexpr int START_WINDOW_HEIGHT = 720; // in pixels
};